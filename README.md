# PhotoServer Feather source

Add this URL in Feather as an AltStore-compatible source:

`https://raw.githubusercontent.com/fuckmmw-afk/PhotoServer-Feather/main/apps.json`

The IPA is unsigned. Install it through Feather with the matching certificate and provisioning profile; do not override its bundle identifier or App Group entitlement.
